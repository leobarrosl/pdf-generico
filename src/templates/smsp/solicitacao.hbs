<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8" />
    <title>Solicitação - {{solicitacao.osNumber}}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            font-size: 14px;
            color: #000;
        }

        h1,
        h2 {
            color: #004AAD;
        }

        .container {
            margin: 20px;
            max-width: 800px;
            border: 1px solid #ccc;
            padding: 25px;
            border-radius: 8px;
        }

        .label {
            font-weight: bold;
            color: #000;
        }

        .categoria {
            display: inline-block;
            padding: 5px 12px;
            border-radius: 5px;
            font-weight: bold;
            color: white;

            background-color: {
                    {
                    solicitacao.categoria.cor
                }
            }

            ;
        }

        .prioridade {
            color: red;
            font-weight: bold;
            font-size: 16px;
        }

        .anexos img {
            max-width: 250px;
            margin-right: 10px;
            margin-bottom: 10px;
            border-radius: 6px;
            border: 1px solid #ddd;
        }

        .page-break {
            page-break-before: always;
        }

        .flex-center {
            display: flex;
            justify-content: center;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>Solicitação Nº {{solicitacao.osNumber}}</h1>
        <div style="display: flex;">
            <div
                style="display: flex; flex-direction: column; gap: 0; padding-right: 35px; border-right: 1px solid #ccc;">
                <div class="section">
                    <p><span class="label">Data da Solicitação:</span> {{formatarData solicitacao.dataSolicitacao}}</p>
                </div>

                <div class="section">
                    <h2>Solicitante</h2>
                    <p><span class="label">Nome:</span> {{solicitacao.nomeSolicitante}}</p>
                    <p><span class="label">Telefone:</span> {{solicitacao.telefoneSolicitante}}</p>
                </div>

                <div class="section">
                    <h2>Serviço</h2>
                    <div>{{solicitacao.categoria.descricao}}
                        <p><span class="label">Sub Grupo:</span> {{#if
                            solicitacao.subGrupo}}{{solicitacao.subGrupo.nomeGrupo}} -
                            {{solicitacao.subGrupo.nome}}{{else}}Nenhum{{/if}}</p>
                        <p><span class="label">Responsável:</span> {{solicitacao.responsavel.nome}}</p>
                    </div>
                </div>

                <div class="section">
                    <h2>Descrição</h2>
                    <p>{{solicitacao.descricao}}</p>
                </div>

                <div class="section">
                    <h2>Endereço</h2>
                    <p><span class="label">Rua:</span> {{solicitacao.endereco.rua}}</p>
                    <p><span class="label">Bairro:</span> {{solicitacao.endereco.bairro}}</p>
                </div>
            </div>
            <div style="display: flex; flex-direction: column; padding-left: 35px;">
                {{#if anexos}}
                <div class="section anexos">
                    <h2>Anexos</h2>
                    {{#each anexos}}
                    {{#if (eq tipoArquivo "Imagem")}}
                    <a href="{{nomeArquivo}}">
                        <img src="{{nomeArquivo}}" alt="Anexo {{@index}}" />
                    </a>
                    {{/if}}
                    {{/each}}
                </div>
                {{/if}}
            </div>
        </div>

        <div class="section mapa">
            <h2>Mapa</h2>
            <a href="https://www.google.com/maps?q={{solicitacao.endereco.latitude}},{{solicitacao.endereco.longitude}}"
                target="_blank">
                <div class="flex-center">
                    <img src="https://maps.googleapis.com/maps/api/staticmap?center={{solicitacao.endereco.latitude}},{{solicitacao.endereco.longitude}}&zoom=16&size=600x300&markers=color:red%7C{{solicitacao.endereco.latitude}},{{solicitacao.endereco.longitude}}&key=AIzaSyCiElcoJ2upbkR0aaR_YGcBdLyF6Q9o4PQ"
                        alt="Mapa" />
                </div>
            </a>
        </div>
    </div>
</body>

</html>